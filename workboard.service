[Unit]
Description=WorkBoard Intranet Dashboard
After=network.target

[Service]
User=root
WorkingDirectory=/root/WorkBoard
EnvironmentFile=/root/WorkBoard/.env
ExecStart=/usr/local/bin/gunicorn -w 2 -b 0.0.0.0:8080 run:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
